# Historical placeholder — no longer describes current architecture

This document is retained only as historical context from the early Phase 1
foundation. It does not describe the current `v0.2.0-beta.1` development
baseline.

## Original placeholder

The memory model is intentionally not implemented in Phase 1.

Future phases can define:

- Event envelopes and append-only log semantics.
- Fact extraction and validation contracts.
- Bi-temporal fact identity and validity windows.
- Active Memory Block selection rules.
- Prompt attachment boundaries and redaction behavior.
