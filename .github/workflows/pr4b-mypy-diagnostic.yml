name: PR4-B Mypy Diagnostic

on:
  push:
    branches:
      - codex/pr4b-full-lite-memory-control-contract
    paths: &diagnostic_paths
      - .github/workflows/pr4b-mypy-diagnostic.yml
  pull_request:
    branches:
      - main
    paths: *diagnostic_paths

jobs:
  mypy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
      - working-directory: memorist-core
        run: uv sync --all-extras
      - name: Capture mypy output
        working-directory: memorist-core
        run: |
          set +e
          uv run mypy src > ../pr4b-mypy.log 2>&1
          status=$?
          cat ../pr4b-mypy.log
          echo "$status" > ../pr4b-mypy.status
          exit 0
      - uses: actions/upload-artifact@v4
        with:
          name: pr4b-mypy-diagnostic
          path: |
            pr4b-mypy.log
            pr4b-mypy.status
